import 'package:flutter/material.dart';

class agtrapport extends StatefulWidget {
  const agtrapport({super.key});

  @override
  State<agtrapport> createState() => _agtrapportState();
}

class _agtrapportState extends State<agtrapport> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        backgroundColor: Colors.white10,
      body:Center(child:Text('Rapport')),
    );
  }
}